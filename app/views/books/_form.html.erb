<%= form_with model: book, local:true do |f| %>
  <% if book.errors.any? %>
    <div class="error_block">
      <h4><%= pluralize(book.errors.count, "error") %> prohibited this book from being saved:</h4>

      <ul>
      <% book.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="title_group">
    <%= f.label :title, class: "label_field" %>
    <%= f.text_field :title, class: "title_field" %>
  </div>

  <div class="body_group">
    <%= f.label :body, class: "label_field" %>
    <%= f.text_area :body, class: "body_field"  %>
  </div>

  <div class="button">
    <%= f.submit %>
  </div>
<% end %>
